DISCORD_BOT_TOKEN=your-discord-bot-token
# Optional default server for Discord scans and commands when server= is omitted
# ATTACK_SCANNER_DEFAULT_SERVER=78
# Optional override if you want the bot and web app to share a non-default database path
# ATTACK_SCANNER_DB=/absolute/path/to/attacks.sqlite3
